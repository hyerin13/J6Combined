<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jhta.mybatis.mapper.cjy.Admin_CancelRefundMapperCjy">

	<select id="list"  resultType="com.jhta.project.vo.cjy.ReservationVo">
		        select * from reservation where rcancel='Y' and rid in(select rid from payment where prefund='N' or prefund is null)
		        order by rid asc
	</select>
	<update id="update" parameterType="map">
		update payment set prefund='Y' where rid=#{rid}
	</update>
	<select id="afterapprovallist" resultType="com.jhta.project.vo.cjy.ReservationVo" >
	select * from reservation where not rid in(select rid from payment where prefund='Y') and rcancel='Y' order by rid asc
	</select>
</mapper>